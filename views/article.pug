extends layout

block content
    h1= article.title

    p= article.body

    a.btn.btn-primary(href="/articles/edit/" + article._id) Edit
    a.btn.btn-danger#delete(href='', data-id= article._id) Delete